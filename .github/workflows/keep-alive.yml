name: Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # Her 5 dakikada bir
  workflow_dispatch:          # Manuel tetikleme için

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping ReOrder
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}" https://reorder-81nz.onrender.com/
